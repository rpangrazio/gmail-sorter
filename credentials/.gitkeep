# Place your OAuth2 credentials.json in this directory.
# Download it from Google Cloud Console → APIs & Services → Credentials.
# This file is tracked so the directory exists in git.
# credentials.json itself is in .gitignore and must NOT be committed.
